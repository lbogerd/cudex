# Typed SQL

Add named PgTyped queries to `*.sql` files in this directory and run
`npm run sql:generate --prefix e2b`. The generated `*.queries.ts` file beside
each SQL source is reviewed and committed.
